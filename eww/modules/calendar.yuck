(defwidget cal []
  (box :class "cal" :orientation "v"
    (box :class "cal-in"
      (calendar :class "cal"
                :day "${current_date.day}"
                :month "${current_date.month}"
                :year "${current_date.year}"))))
